# 2020cce

## 第一題
進階題：反序數字 : 輸入1個正整數，將該整數所有數字反序排列後組成一個的新整數，計算出兩者相加的結果。 
數字範圍：整數 1 – 10000  
```C
#include <stdio.h>
int main()
{
    int n,i,a,b,c,d,e;
    scanf("%d",&n);
    
    a=n/10000;
    b=(n%10000)/1000;
    c=((n%10000)%1000)/100;
    d=((((n%10000)%1000))%100)/10;
    e=(((n%10000)%1000)%100)%10;
    
    if(n>=0 && n<=9)
    {
        printf("%d+%d=%d\n",n,n,n+n);
    }
    else if(n>=10 && n<=99)
    {
        i=e*10+d*1;
        printf("%d+%d=%d\n",n,i,n+i);
    }
    else if(n>=100 && n<=999)
    {
        i=e*100+d*10+c*1;
        printf("%d+%d=%d\n",n,i,n+i);
    }
    else if(n>=1000 && n<=9999)
    {
        i=e*1000+d*100+c*10+b*1;
        printf("%d+%d=%d\n",n,i,n+i);
    }
    else if(n>=10000 && n<=99999)
    {
        i=e*10000+d*1000+c*100+b*10+a*1;
        printf("%d+%d=%d\n",n,i,n+i);
    }
    
    return 0;
}
```

## 第二題
進階題：絕對值函數 : 
```C


```

## 第三題
基礎題：N數之和 : 輸入一個整數N，之後讀入N個整數，請輸出其和。 
數字範圍：N=整數1 – 10，其餘整數1 – 100。
```C
#include <stdio.h>
int main()
{
    int n,j,sum=0;
    scanf("%d",&n);
    
    for(int i=0;i<n;i++)
    {
        scanf("%d",&j);
        sum+=j;
    }
 
    printf("%d\n",sum);

}
```

## 第四題
基礎題：三數極大 : 輸入三個正整數，輸出其最大值。 
數字範圍：整數1 – 100 
```C
#include <stdio.h>
int main()
{
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c);

    if(a>=b && a>=c && b>=c)
    {
        printf("%d\n",a);
    }
    else if(a>=b && a>=c && c>=b)
    {
        printf("%d\n",a);
    }
    else if(b>=a && b>=c && a>=c)
    {
        printf("%d\n",b);
    }
    else if(b>=a && b>=c && c>=a)
    {
        printf("%d\n",b);
    }
    else if(c>=a && c>=b && a>=b)
    {
        printf("%d\n",c);
    }
    else if(c>=a && c>=b && b>=a)
    {
        printf("%d\n",c);
    }
    
}
```

## 第五題
基礎題：計算商數 : 輸入兩個整數a，b，輸出a除以b的商。 
數字範圍：整數 1 – 10000
```C
#include <stdio.h>
int main()
{
    int a,b,i;
    scanf("%d%d",&a,&b);
    
    i=a/b;
    printf("%d\n",i);

}
```
